server:
  port: 8083

spring:
  application:
    name: faturamento

minio:
  url: 'http://localhost:9000'
  access-key: ${ACCESS-MINIO:minioadmin}
  secret-key: ${SECRET-MINIO:minioadmin123}
  bucket: icompras.faturamento
  path: faturas
